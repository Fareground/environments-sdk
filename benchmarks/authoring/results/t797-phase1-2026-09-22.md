# Authoring benchmark — replay of t797-phase1-2026-09-22

SDK commit `b8299ec` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide parts |
|---|---|---|---|---|---|---|---|---|
| auction_house | yes (4) | 6/6 | 4/4 | 567,109/74,865 | $0.067 | 989 | 472 | all, effects, functions, actions, stages, events, population, metrics, model, inputs, types, outputs, policies, expressions, views, clock, functions.collections, brief, invariants |
| beer_game | yes (1) | 3/3 | 0/6 | 486,354/129,352 | $0.128 | 1322 | 40 | all, patterns, functions.collections, events, expressions, metrics, types, actions, stages, brief, effects, functions, functions.stats, inputs, defs, views, world, outputs |
| classroom | yes (2) | 12/12 | 3/4 | 529,962/76,010 | $0.040 | 1101 | 404 | all, population, actions, stages, events, functions.collections, outputs, expressions, effects, views, types, brief, functions, functions.random, model, policies, inputs, metrics, authoring, inspect |
| ed_triage | yes (1) | 6/9 | 4/4 | 499,227/105,214 | $0.053 | 1508 | 403 | all, patterns, operations.queue, effects, functions, functions.collections, expressions, inputs, functions.random, model, invariants, events, population, views, stages, actions, outputs, clock, metrics, types, patterns.draw, defs, policies |
| jury_trial | yes (2) | 21/21 | 4/4 | 519,785/83,995 | $0.061 | 632 | 610 | all, actions, stages, events, functions.collections, views, brief, outputs, population, inputs, expressions, functions, model, inspect, policies, authoring |
| labor_negotiation | yes (1) | 12/12 | 4/4 | 516,462/123,366 | $0.061 | 1727 | 602 | all, actions, stages, events, outputs, effects, inputs, agreements.negotiation, functions.collections, population, brief, views, model, entities, types, invariants, world, records, templates, end, expressions, authoring, inspect, policies |
| sealed_tender | yes (2) | 24/24 | 4/4 | 596,857/48,709 | $0.045 | 454 | 434 | all, stages, actions, views, mechanisms, social, groups, records, functions, functions.collections, effects, events, population, inputs, brief, types, world, outputs, model, functions.world, functions.lists, expressions, policies, inspect |
| sports_league | yes (1) | 3/3 | 4/4 | 321,816/43,163 | $0.033 | 427 | 571 | all, patterns, expressions, effects, outputs, events, stages, population, inputs, types, metrics, patterns.counts, functions.collections, views, brief, defs, functions.stats, counts, functions, functions.random |
| town_hall | yes (1) | 6/6 | 4/4 | 522,585/90,223 | $0.052 | 1273 | 437 | all, decision, stages, expressions, effects, views, types, records, entities, functions, functions.collections, defs, population, outputs, inputs, actions, events, model, metrics, brief, clock, end, invariants, templates, functions.text, policies |
| treaty | yes (1) | 9/9 | 4/4 | 273,951/23,641 | $0.020 | 244 | 254 | all, stages, patterns, functions.collections, types, inputs, end, events, actions, views, effects, policies |
| weekly_inventory | yes (2) | 9/9 | 0/4 | 546,560/71,831 | $0.039 | 1033 | 393 | patterns, functions, effects, events, patterns.counts, patterns.draw, inputs, population, all, functions.collections, economy, functions.economy, stages, economy.demand, economy.replenishment, policies, views, outputs, world, invariants, functions.world |
| werewolf | yes (1) | 3/3 | 4/4 | 595,220/47,170 | $0.040 | 447 | 343 | all, actions, views, types, records, stages, events, end, functions.collections, mechanisms.decision, mechanisms.groups, mechanisms.social, effects, mechanisms, groups, decision, social, mind, groups.roles, decision.ballot, flow.victory, population, inputs, outputs, metrics |

**Aggregate:** clean check 12/12 · all runs ok 11/12 · fidelity 39/50 (78%) · tokens 5,975,888 in / 917,539 out · $0.639

## Check errors authors hit (most frequent first)

- 2× `$actor.has_bid: collector has no property '…'` — e.g. `actions.sealed_bid.when[3]: $actor.has_bid: collector has no property 'has_bid' → properties: cash — in `not $actor.has_bid``
- 2× `$it.bid: collector has no property '…'` — e.g. `events[2].do[0].if: $it.bid: collector has no property 'bid' → properties: cash — in `$len($top(collector, $it.bid, 1, $it.has_bid)) > 0``
- 2× `$it.has_bid: collector has no property '…'` — e.g. `events[2].do[0].if: $it.has_bid: collector has no property 'has_bid' → properties: cash — in `$len($top(collector, $it.bid, 1, $it.has_bid)) > 0``
- 2× `'…' is not a field here` — e.g. `records.gradebook.title: 'title' is not a field here → 'title' belongs to another part of the contract; remove it here`
- 1× `invariant `$sum(collector, $it.cash) + $entity(house).cash == $count(collector) * $inputs.starting_cash` no longer holds after round (Cash is conserved between the collectors and the house.) (smoke run of N round(s), random agents)` — e.g. `invariants[0]: invariant `$sum(collector, $it.cash) + $entity(house).cash == $count(collector) * $inputs.starting_cash` no longer holds after round (Cash is conserved between the collectors and the house.) (smoke run of `
- 1× `invariant `$sum(collector, $it.cash) + $entity(house).cash == $count(collector) * $inputs.starting_cash` no longer holds after the run (Cash is conserved between the collectors and the house.) (smoke run of N round(s), random agents)` — e.g. `invariants[0]: invariant `$sum(collector, $it.cash) + $entity(house).cash == $count(collector) * $inputs.starting_cash` no longer holds after the run (Cash is conserved between the collectors and the house.) (smoke run o`
- 1× `$actor.bid: collector has no property '…'` — e.g. `actions.sealed_bid.do[1]: $actor.bid: collector has no property 'bid' → properties: cash — in `$actor.bid = $params.amount``
- 1× `$actor.bid_seq: collector has no property '…'` — e.g. `actions.sealed_bid.do[2]: $actor.bid_seq: collector has no property 'bid_seq' → properties: cash — in `$actor.bid_seq = $world.seq``
- 1× `$it is not available here` — e.g. `policies.doctor.rules[0].with.patient: $it is not available here → available: $actor, $arm, $clock, $inputs, $metrics, $pattern, $pending, $physics, $round, $series, $stage, $world — in `$best(patient, [$it.urgency, $it.`
- 1× `wrong number of arguments: $best(items, by, ties?)` — e.g. `policies.doctor.rules[0].with.patient: wrong number of arguments: $best(items, by, ties?) → in `$best(patient, [$it.urgency, $it.arrived_round, $it.id], 1, $it.triaged and $is_waiting($it))[0].id``
- 1× `patient '…' has no property '…' (has: arrived_round, left, left_round, seen_round, treated, treated_round, treating_from, triaged, urgency) — in `$best($filter(patient, $it.triaged and $is_waiting($it)), [$it.urgency, $it.arrived_round, $it.id])[N].id` (smoke run of N round(s), policy '…' playing doctor)` — e.g. `policies.doctor.rules[0]: patient 'patient_2' has no property '0' (has: arrived_round, left, left_round, seen_round, treated, treated_round, treating_from, triaged, urgency) — in `$best($filter(patient, $it.triaged and $`
- 1× `syntax error: expected '…' after '…' expression` — e.g. `outputs.verdict: syntax error: expected 'else' after 'if' expression → expression: "guilty" if $all(juror, $it.has_voted and $it.vote == guilty) else ("not guilty" if $all(juror, $it.has_voted and $it.vote == not guilty)`
- 1× `syntax error: invalid syntax` — e.g. `invariants[1]: syntax error: invalid syntax → expression: $all(juror, not $it.has_voted or $it.vote == guilty or $it.vote == not guilty)`
- 1× `must be text, got an object` — e.g. `records.collusion.fields.text: must be text, got an object`
- 1× `template "The ballots are counted. {$map(proposal, $it.title + '…' + $it.result + '…' + $it.yes + '…' + $it.no + '…' + $it.abstain + '…')|list}": expected a number, got str '…' — in `$map(proposal, $it.title + '…' + $it.result + '…' + $it.yes + '…' + $it.no + '…' + $it.abstain + '…')` (smoke run of N round(s), random agents)` — e.g. `events[2].say: template "The ballots are counted. {$map(proposal, $it.title + ': ' + $it.result + ' (yes ' + $it.yes + ', no ' + $it.no + ', abstain ' + $it.abstain + ')')|list}": expected a number, got str 'New park: no`
- 1× `unknown format '…')' (use one of: money, pct, pctN, int, upper, lower, title, yesno, list, N, N, N, N, N)` — e.g. `events[2].say: unknown format '')' (use one of: money, pct, pct1, int, upper, lower, title, yesno, list, 0, 1, 2, 3, 4) → template: The ballots are counted. {$join($map(proposal, $it.title + ' — ' + $it.result + ' (yes '`
- 1× `invariants[N]: expected a number, got null — in `$entity(owner).cash_cents + $entity(supplier).cash_cents == $world.opening_cash_cents + $world.receipts_cents`` — e.g. `invariants[2]: invariants[2]: expected a number, got null — in `$entity(owner).cash_cents + $entity(supplier).cash_cents == $world.opening_cash_cents + $world.receipts_cents` → fix the rule at this path (found by a smoke`
- 1× `invariants[N]: invariant `$count(player, $it.role == '…') == N and $count(player, $it.role == '…') == N` no longer holds after build (Two werewolves, one seer, always.)` — e.g. `invariants[2]: invariants[2]: invariant `$count(player, $it.role == 'werewolf') == 2 and $count(player, $it.role == 'seer') == 1` no longer holds after build (Two werewolves, one seer, always.) → fix the rule at this pat`

## Failed fidelity checks

- **beer_game** `outputs_present`: missing required outputs ['customer_demand', 'factory_orders', 'costs_by_role', 'total_team_cost', 'bullwhip_ratio'] (has ['note'])
- **beer_game** `beer_demand_is_random_and_in_range`: KeyError: 'customer_demand'
- **beer_game** `beer_demand_is_exogenous`: KeyError: 'customer_demand'
- **beer_game** `beer_costs_add_up`: KeyError: 'costs_by_role'
- **beer_game** `beer_idle_team_pays_backlog`: KeyError: 'total_team_cost'
- **beer_game** `beer_bullwhip_matches_its_definition`: KeyError: 'customer_demand'
- **classroom** `class_scores_are_valid`: 2 students, expected 5
- **ed_triage** run {'doctor': 'policy:doctor'} seed 1 failed: policies.doctor.rules[0]: patient 'patient_1' has no property '0' (has: arrived_round, left, left_round, seen_round, treated, treated_round, treating_from, triaged, urgency) — in `$best($filter(patient, $it.triaged and $is_waiting($it)), [$it.urgency, $it.arrived_round, $it.id])[0].id`
- **ed_triage** run {'doctor': 'policy:doctor'} seed 2 failed: policies.doctor.rules[0]: patient 'patient_3' has no property '0' (has: arrived_round, left, left_round, seen_round, treated, treated_round, treating_from, triaged, urgency) — in `$best($filter(patient, $it.triaged and $is_waiting($it)), [$it.urgency, $it.arrived_round, $it.id])[0].id`
- **ed_triage** run {'doctor': 'policy:doctor'} seed 3 failed: policies.doctor.rules[0]: patient 'patient_1' has no property '0' (has: arrived_round, left, left_round, seen_round, treated, treated_round, treating_from, triaged, urgency) — in `$best($filter(patient, $it.triaged and $is_waiting($it)), [$it.urgency, $it.arrived_round, $it.id])[0].id`
- **weekly_inventory** `outputs_present`: missing required outputs ['units_ordered', 'units_sold', 'units_on_hand_end', 'units_on_order_end', 'lost_sales', 'demand_by_week'] (has ['cash_end', 'delivery_events', 'on_hand_end', 'on_order_end', 'ordered', 'orders', 'received'])
- **weekly_inventory** `shop_stock_is_conserved`: KeyError: 'units_ordered'
- **weekly_inventory** `shop_cash_is_conserved`: KeyError: 'units_sold'
- **weekly_inventory** `shop_demand_is_exogenous`: KeyError: 'demand_by_week'
