# Authoring benchmark — replay of r2-s2-2026-09-23

SDK commit `bdd8587` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide tokens | guide parts |
|---|---|---|---|---|---|---|---|---|---|
| auction_house | yes (4) | 0/3 | 0/4 | 495,967/118,406 | $0.059 | 1586 | 505 | 14,259 | guide, mechanisms, stages, actions, expressions, effects, functions.collections, views, defs, types, population, events, market, market.auction, policies, outputs, brief, inputs |
| beer_game | yes (2) | 6/6 | 0/6 | 467,338/143,373 | $0.069 | 1908 | 366 | 12,165 | effects, expressions, functions.collections, stages, events, outputs, functions.random, brief, population, views, inputs, clock, end, metrics, policies, invariants, actions, inspect, world |
| classroom | yes (3) | 3/3 | 4/4 | 155,751/13,528 | $0.018 | 94 | 206 | 9,086 | views, actions, stages, events, effects, outputs, functions.collections, policies, expressions |
| ed_triage | yes (1) | 12/12 | 4/4 | 539,862/83,149 | $0.047 | 1157 | 404 | 12,549 | patterns, events, effects, functions.random, actions, stages, views, policies, functions.collections, patterns.counts, patterns.draw, outputs, inputs, population, metrics, brief |
| jury_trial | yes (2) | 3/3 | 4/4 | 580,741/61,198 | $0.089 | 230 | 459 | 11,610 | stages, actions, effects, events, views, records, outputs, inputs, population, functions.collections, types, expressions, guide, inspect, end, policies |
| labor_negotiation | yes (2) | 3/3 | 4/4 | 519,336/57,026 | $0.084 | 196 | 442 | 9,283 | stages, actions, effects, expressions, events, end, views, outputs, inputs, population, policies |
| sealed_tender | yes (3) | 6/6 | 4/4 | 468,586/33,911 | $0.050 | 442 | 372 | 17,012 | mechanisms, records, patterns, stages, market.auction, groups.roles, social.channels, functions.collections, expressions, effects, outputs, events, inputs, population, types, functions.random, groups, policies |
| sports_league | yes (1) | 3/3 | 4/4 | 210,450/40,660 | $0.038 | 450 | 458 | 11,091 | guide, stages, actions, expressions, functions.random, defs, population, outputs, functions.collections, effects, inputs, events, views, types, metrics, brief |
| town_hall | yes (1) | 12/12 | 4/4 | 567,806/59,232 | $0.032 | 831 | 316 | 13,576 | stages, actions, effects, expressions, views, outputs, mechanisms, inputs, decision.ballot, records, events, population, functions.collections, clock, functions.random, metrics, end, inspect, policies |
| treaty | yes (3) | 6/6 | 3/4 | 573,153/56,779 | $0.026 | 397 | 325 | 16,527 | patterns, functions.random, population, types, records, actions, effects, stages, functions.collections, views, outputs, expressions, inputs, end, entities, world, metrics, brief, clock, invariants, guide, functions.text, mechanisms, policies |
| weekly_inventory | yes (1) | 6/6 | 4/4 | 377,628/34,607 | $0.043 | 456 | 370 | 17,365 | patterns, stages, effects, actions, outputs, views, functions.random, inputs, events, world, metrics, policies, types, population, patterns.counts, guide, patterns.draw, expressions, functions.collections, patterns |
| werewolf | yes (2) | 15/15 | 4/4 | 566,659/41,864 | $0.064 | 320 | 498 | 11,939 | stages, actions, events, views, end, mechanisms, outputs, functions.collections, groups.roles, decision.ballot, effects, population, inputs, records, policies, functions.random |

**Aggregate:** clean check 12/12 · all runs ok 11/12 · fidelity 39/50 (78%) · fidelity of the contract `fg_env.author` saves 40/50 (80%) · tokens 5,523,277 in / 743,733 out · $0.618

## Check errors authors hit (most frequent first)

- 4× `$it.ratified: nation has no property '…'` — e.g. `metrics.ratified: $it.ratified: nation has no property 'ratified' → properties: proposed, red_line — in `$count(nation, $it.ratified)``
- 3× `$actor is not available here` — e.g. `stages[0].who: $actor is not available here → available: $arm, $clock, $i, $inputs, $it, $metrics, $pattern, $pending, $physics, $round, $series, $stage, $world — in `$world.high_bidder != $actor.id and $actor.cash >= $w`
- 2× `'…' needs a subject; write {$actor.params.effort} or similar` — e.g. `actions.study.outcome: '{params.effort}' needs a subject; write {$actor.params.effort} or similar → template: You will study {params.effort} this week.`
- 2× `$world.ratifications: no such world property` — e.g. `actions.propose.do[2]: $world.ratifications: no such world property → declare it under `world``
- 1× `'…' has no property '…' (declared: lot, format, open, closed, sold, winner, price, high_bid, high_bidder, bids, seqs, bid_submitted) (smoke run of N round(s), random agents)` — e.g. `painting.open_round: 'painting' has no property 'open_round' (declared: lot, format, open, closed, sold, winner, price, high_bid, high_bidder, bids, seqs, bid_submitted) (smoke run of 1 round(s), random agents) → fix the`
- 1× `template '…': expected a number, got str '…' — in `high_bid + $inputs.min_increment` (smoke run of N round(s), random agents)` — e.g. `views.on_the_block: template 'Lot {lot} is open for bids. High bid {high_bid|money} by {high_bidder}. Minimum raise: {high_bid + $inputs.min_increment|money}. This painting is worth {$get($actor.valuations, $it.lot - 1, `
- 1× `$it.bids: painting has no property '…'` — e.g. `stages[1].who: $it.bids: painting has no property 'bids' → properties: closed, format, high_bid, high_bidder, lot, open, open_round, price, sold, winner — in `$it.cash > 0 and $any(painting, $it.format == sealed and $it.`
- 1× `unexpected `{` on the left side of the assignment` — e.g. `actions.probe.do[1]: unexpected `{` on the left side of the assignment → write `$actor.cash -= 5`, `$world.board[$i][$j] = x` or `$total = 3``
- 1× `$slice: '…' is not an entity type (pass a type name or a list) — in `$slice($world.pipe[$it.id], N)` (smoke run of N round(s), random agents)` — e.g. `events[0].do[1]: $slice: '$map($range($inputs.lead_time), 4)' is not an entity type (pass a type name or a list) — in `$slice($world.pipe[$it.id], 1)` (smoke run of 1 round(s), random agents) → fix the rule at this path `
- 1× `'…' is not a declared type` — e.g. `actions.produce.by: 'factory' is not a declared type → types: company`
- 1× `'…' needs a subject; write {$actor.backlog} or similar` — e.g. `actions.ship.outcome: '{backlog}' needs a subject; write {$actor.backlog} or similar → template: You shipped {$params.quantity} cases and still owe {backlog} cases.`
- 1× `$sum(pipeline, …): '…' is not a declared type` — e.g. `views.your company.show: $sum(pipeline, …): 'pipeline' is not a declared type → types: company`
- 1× `cannot read '…' of null — in `$all(student, $all($outer.scores, $it >= N and $it <= N))`` — e.g. `invariants[0]: cannot read '.scores' of null — in `$all(student, $all($outer.scores, $it >= 0 and $it <= 10))` → fix the rule at this path (found by a smoke run)`
- 1× `'…' is a built-in entity field` — e.g. `types.exhibit.props.name: 'name' is a built-in entity field → remove name from props; set name on the entity or population entry, outside props`
- 1× `syntax error: invalid syntax` — e.g. `stages[1].order: syntax error: invalid syntax → expression: $it.type == (union_negotiator if $round % 2 == 1 else company_negotiator) ? 0 : 1`
- 1× `can only assign to an entity'…'s field, $world.x or $physics.x (`$it.colluder = true`) (smoke run of N round(s), random agents)` — e.g. `events[0].do[1].do[0]: can only assign to an entity's property, a link's field, $world.x or $physics.x (`$it.colluder = true`) (smoke run of 1 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `cannot read '…' of str '…' — in `$world.ranked[N].bid` (smoke run of N round(s), random agents)` — e.g. `events[1].do[2].else[0].then[1]: cannot read '.bid' of str 'firm_2' — in `$world.ranked[1].bid` (smoke run of 1 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `views.your matchday fixtures: template '…': no field '…' (fields: away, home) — in `$it.a` (smoke run of N round(s), random agents)` — e.g. `(run): views.your matchday fixtures: template '{a} (home) v {b} (away)': no field 'a' (fields: away, home) — in `$it.a` (smoke run of 1 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `not valid JSON` — e.g. `(contract): not valid JSON`
- 1× `$actor.ratified: nation has no property '…'` — e.g. `actions.ratify.do[0].then[1]: $actor.ratified: nation has no property 'ratified' → properties: proposed, red_line — in `$actor.ratified = true``
- 1× `$world.ratified_names: no such world property` — e.g. `views.table.show: $world.ratified_names: no such world property → declare it under `world``
- 1× `$world.in_force: no such world property` — e.g. `outputs.in_force: $world.in_force: no such world property → declare it under `world``
- 1× `$world.agreed_cut: no such world property` — e.g. `outputs.agreed_cut: $world.agreed_cut: no such world property → declare it under `world``
- 1× `filters the choices by private role of other player agents: the tool's list of choices would reveal it to the actor` — e.g. `actions.wolf_kill.params.target.where: filters the choices by private role of other player agents: the tool's list of choices would reveal it to the actor → filter by what the actor may know (public properties, its own, `
- 1× `$sort: sort key must be a number, text, or a list of them, got bool True — in `$sort($filter(player, $it.in_game and $it.id != $actor.id), [$it.id > $actor.id, $it.id])[N]` (smoke run of N round(s), policy '…' playing player)` — e.g. `policies.cyclic.rules[3]: $sort: sort key must be a number, text, or a list of them, got bool True — in `$sort($filter(player, $it.in_game and $it.id != $actor.id), [$it.id > $actor.id, $it.id])[0]` (smoke run of 1 round`

## Failed fidelity checks

- **auction_house** `outputs_present`: random runs fail
- **auction_house** `auction_lots_are_well_formed`: random runs fail
- **auction_house** `auction_money_is_conserved`: random runs fail
- **auction_house** `auction_english_lots_sell`: random runs fail
- **auction_house** run random seed 1 failed: ContractError: contract is invalid (1 error(s)):
  - stages[1].who: $it.bids: painting has no property 'bids' → properties: closed, format, high_bid, high_bidder, lot, open, open_round, price, sold, winner — in `$it.cash > 0 and $any(painting, $it.format == sealed and $it.open and not $it.closed and
- **auction_house** run random seed 2 failed: ContractError: contract is invalid (1 error(s)):
  - stages[1].who: $it.bids: painting has no property 'bids' → properties: closed, format, high_bid, high_bidder, lot, open, open_round, price, sold, winner — in `$it.cash > 0 and $any(painting, $it.format == sealed and $it.open and not $it.closed and
- **auction_house** run random seed 3 failed: ContractError: contract is invalid (1 error(s)):
  - stages[1].who: $it.bids: painting has no property 'bids' → properties: closed, format, high_bid, high_bidder, lot, open, open_round, price, sold, winner — in `$it.cash > 0 and $any(painting, $it.format == sealed and $it.open and not $it.closed and
- **beer_game** `outputs_present`: missing required outputs ['customer_demand', 'factory_orders', 'total_team_cost', 'bullwhip_ratio'] (has ['costs_by_role', 'hist'])
- **beer_game** `beer_demand_is_random_and_in_range`: KeyError: 'customer_demand'
- **beer_game** `beer_demand_is_exogenous`: KeyError: 'customer_demand'
- **beer_game** `beer_costs_add_up`: KeyError: 'total_team_cost'
- **beer_game** `beer_idle_team_pays_backlog`: KeyError: 'total_team_cost'
- **beer_game** `beer_bullwhip_matches_its_definition`: KeyError: 'customer_demand'
- **treaty** `treaty_red_lines_stay_secret`: Norland was shown Westmark's red line 48: …- Suria puts a proposal of a 11% emissions cut on the table.
- Westmark puts a proposal of a 48% emissions cut on the table.

The table: The proposal on the table is a 48% emissions c…
