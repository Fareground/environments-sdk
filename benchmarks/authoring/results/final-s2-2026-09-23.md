# Authoring benchmark — final-s2-2026-09-23

SDK commit `bd69a38` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide tokens | guide parts |
|---|---|---|---|---|---|---|---|---|---|
| auction_house | yes (2) | 3/3 | 4/4 | 314,293/157,581 | $0.073 | 1918 | 401 | 14,059 | stages, population, actions, mechanisms, effects, records, outputs, events, market.auction, types, inputs, views, functions.collections, functions.random, expressions, guide, world, end, brief, invariants, policies, metrics |
| beer_game | yes (2) | 9/9 | 5/6 | 412,366/145,442 | $0.067 | 1805 | 399 | 17,095 | guide, effects, stages, events, actions, expressions, views, inputs, outputs, patterns, mechanisms, population, functions, functions.random, types, entities, functions.collections, functions.stats, metrics, policies |
| classroom | yes (1) | 9/9 | 4/4 | 223,443/17,110 | $0.014 | 101 | 253 | 9,004 | stages, actions, views, outputs, functions.collections, effects, events, expressions, policies |
| ed_triage | yes (3) | 0/9 | 0/4 | 557,444/81,306 | $0.043 | 1151 | 471 | 19,716 | patterns, functions.random, effects, stages, events, outputs, functions.collections, views, actions, policies, inputs, population, brief, types, invariants, metrics, patterns.noise, patterns.counts, patterns.draw, patterns.series, inspect, clock, end, entities, overview, guide, map, functions, patterns.observation, patterns.random, patterns.population, patterns.time, patterns, expressions |
| jury_trial | yes (2) | 18/18 | 4/4 | 368,989/61,747 | $0.045 | 870 | 630 | 12,227 | stages, actions, events, records, views, effects, expressions, functions.collections, entities, outputs, invariants, inputs, population, clock, end, types, policies, world, inspect |
| labor_negotiation | yes (1) | 21/21 | 4/4 | 530,909/108,389 | $0.054 | 1341 | 679 | 13,664 | guide, stages, actions, views, mechanisms, end, records, outputs, agreements.negotiation, effects, inputs, population, expressions, functions.collections, brief, events, policies, invariants, inspect |
| sealed_tender | yes (1) | 12/12 | 4/4 | 248,994/32,089 | $0.022 | 172 | 394 | 13,721 | functions.collections, functions.random, records, stages, mechanisms, effects, actions, views, inputs, outputs, events, policies, population, types, expressions, end, clock, brief, social.channels |
| sports_league | yes (2) | 3/3 | 4/4 | 261,700/41,300 | $0.028 | 155 | 683 | 15,347 | inputs, population, stages, actions, effects, functions.collections, outputs, views, entities, patterns, events, expressions, clock, metrics, end, records, functions.random, defs, blocks, types, brief, policies, invariants |
| town_hall | yes (2) | 12/12 | 4/4 | 531,908/74,520 | $0.052 | 602 | 443 | 12,718 | stages, mechanisms, records, actions, decision.ballot, views, outputs, expressions, effects, events, functions.collections, population, policies, defs, types, end, population.mix, inputs |
| treaty | yes (3) | 9/9 | 3/4 | 553,569/52,126 | $0.032 | 538 | 329 | 13,863 | patterns, stages, inputs, types, actions, views, events, outputs, records, functions.collections, effects, end, population, functions.random, invariants, brief, blocks, defs, policies |
| weekly_inventory | yes (2) | 9/9 | 4/4 | 371,208/30,287 | $0.024 | 481 | 309 | 12,997 | effects, patterns, types, population, outputs, stages, views, actions, patterns.observation, inputs, patterns.counts, functions.collections, events, policies, patterns.counts, patterns.draw, functions.random, end, guide |
| werewolf | yes (3) | 9/9 | 4/4 | 575,181/67,546 | $0.049 | 558 | 479 | 17,280 | guide, mechanisms, effects, expressions, stages, actions, types, outputs, end, views, records, events, brief, population, policies, groups.roles, functions.random, functions.collections, decision.ballot, patterns, inspect, inputs |

**Aggregate:** clean check 12/12 · all runs ok 11/12 · fidelity 44/50 (88%) · tokens 4,950,004 in / 869,443 out · $0.502

## Check errors authors hit (most frequent first)

- 2× `not valid JSON` — e.g. `(contract): not valid JSON`
- 2× `'…' is not a field here` — e.g. `policies.fractions_hard.for: 'for' is not a field here → 'for' belongs to another part of the contract; remove it here; see guide('policies')`
- 2× `must be text, got an object` — e.g. `policies.fractions_hard.rules[0].do: must be text, got an object → see guide('policies')`
- 2× `$it is not available here` — e.g. `events[0].say: $it is not available here → available: $arm, $clock, $inputs, $metrics, $pattern, $pending, $physics, $round, $series, $stage, $world — in `$entity($it.home).name``
- 1× `must be a number, got the text "$inputs.increment"` — e.g. `actions.bid.params.amount.step: must be a number, got the text "$inputs.increment" → write a number without quotes; see guide('actions')`
- 1× `$pattern.arr: '…' is not one of its keys — in `$pattern.arr($inputs.rate, "{$round}-N")` (smoke run of N round(s), random agents)` — e.g. `events[0].do[0]: $pattern.arr: '{$round}-1' is not one of its keys — in `$pattern.arr($inputs.rate, "{$round}-1")` (smoke run of 1 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `expected a number, got str '…' — in `$pattern.arr($inputs.rate, $round + '…')` (smoke run of N round(s), random agents)` — e.g. `events[0].do[0]: expected a number, got str '-1' — in `$pattern.arr($inputs.rate, $round + '-1')` (smoke run of 1 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.) (smoke run of N round(s), random agents)` — e.g. `invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.) (smoke run of 5 round(s), random agents) → fix the rule a`
- 1× `$records is not available here` — e.g. `views.closings.of: $records is not available here → available: $actor, $arm, $clock, $inputs, $metrics, $pattern, $pending, $physics, $round, $series, $stage, $world — in `$records.closings``
- 1× `must be a whole number, got the text "$inputs.speeches_per_round"` — e.g. `actions.speak.per_round: must be a whole number, got the text "$inputs.speeches_per_round" → write a whole number without quotes; see guide('actions')`
- 1× `must be a number, got the text "$inputs.red_line_min"` — e.g. `types.nation.props.red_line.min: must be a number, got the text "$inputs.red_line_min" → write a number without quotes; see guide('types')`
- 1× `must be a number, got the text "$inputs.red_line_max"` — e.g. `types.nation.props.red_line.max: must be a number, got the text "$inputs.red_line_max" → write a number without quotes; see guide('types')`
- 1× `$i is not available here` — e.g. `population[0].where: $i is not available here → available: $arm, $clock, $inputs, $metrics, $pattern, $pending, $physics, $round, $row, $series, $stage, $world — in `$i <= $inputs.nations``
- 1× `'…' is a built-in entity field` — e.g. `types.product.props.name: 'name' is a built-in entity field → remove name from props; set name on the entity or population entry, outside props`
- 1× `'…' needs a subject; write {$actor.cost} or similar` — e.g. `actions.order.outcome: '{cost}' needs a subject; write {$actor.cost} or similar → template: You ordered {$params.qty} x {$params.product.name} for ${cost|money}. It arrives next week. Cash left: ${cash|money}.`
- 1× `'…'alive'…'dead'…' is not a field name; write an expression as {$...}` — e.g. `views.village.show: '{living and 'alive' or 'dead'}' is not a field name; write an expression as {$...} → template: {name} — {living and 'alive' or 'dead'}`
- 1× `invariant `$count(player, $it.role == werewolf) == $inputs.werewolves` no longer holds after build (There are exactly the declared number of werewolves.)` — e.g. `invariants[1]: invariant `$count(player, $it.role == werewolf) == $inputs.werewolves` no longer holds after build (There are exactly the declared number of werewolves.) → fix the rule at this path (found by a smoke run)`

## Failed fidelity checks

- **beer_game** `beer_bullwhip_matches_its_definition`: 19 factory orders vs 20 demand weeks
- **ed_triage** `outputs_present`: random runs fail
- **ed_triage** `ed_patients_are_conserved`: random runs fail
- **ed_triage** `ed_arrivals_are_exogenous`: random runs fail
- **ed_triage** `ed_idle_staff_treat_nobody`: random runs fail
- **ed_triage** run random seed 1 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run random seed 2 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run random seed 3 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:sickest_first', 'triage_nurse': 'policy:sickest_first'} seed 1 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:sickest_first', 'triage_nurse': 'policy:sickest_first'} seed 2 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:sickest_first', 'triage_nurse': 'policy:sickest_first'} seed 3 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:first_come_first_served', 'triage_nurse': 'policy:first_come_first_served'} seed 1 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:first_come_first_served', 'triage_nurse': 'policy:first_come_first_served'} seed 2 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **ed_triage** run {'doctor': 'policy:first_come_first_served', 'triage_nurse': 'policy:first_come_first_served'} seed 3 failed: invariants[2]: invariant `$all($filter(patient, $it.status == left), $it.seen == false)` no longer holds after round (Only patients who were never seen walk out.)
- **treaty** `treaty_red_lines_stay_secret`: Norland was shown Westmark's red line 14: …t.
- You ratify the 14% cut.
- Estavia ratifies the 14% cut.
- Westmark ratifies the 14% cut.
- The treaty enters into force: every signatory cuts its e…
