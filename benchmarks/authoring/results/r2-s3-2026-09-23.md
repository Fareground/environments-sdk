# Authoring benchmark — replay of r2-s3-2026-09-23

SDK commit `bdd8587` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide tokens | guide parts |
|---|---|---|---|---|---|---|---|---|---|
| auction_house | yes (3) | 6/6 | 4/4 | 456,596/163,206 | $0.077 | 2131 | 505 | 14,156 | mechanisms, stages, actions, effects, market.auction, views, events, expressions, functions.collections, population, outputs, inputs, policies, guide, functions.random, inspect, types, end, records |
| beer_game | yes (1) | 12/12 | 6/6 | 239,644/118,440 | $0.054 | 1641 | 497 | 12,612 | actions, effects, expressions, functions.collections, stages, views, events, outputs, inputs, population, policies, inspect, types, entities, brief, clock, metrics, invariants, defs, functions.random |
| classroom | yes (1) | 3/3 | 4/4 | 83,561/10,113 | $0.011 | 164 | 242 | 9,663 | guide, stages, actions, effects, views, events, outputs, population, functions.collections, functions.random, types, inputs |
| ed_triage | yes (3) | 9/9 | 4/4 | 437,913/97,684 | $0.050 | 1378 | 428 | 16,309 | effects, patterns, functions.random, stages, population, outputs, actions, views, events, patterns.counts, patterns.draw, expressions, policies, metrics, functions.collections, inspect, None, inputs, invariants, defs, world, clock, brief, index, counts, draw, patterns_kinds, pattern.counts, mechanisms |
| jury_trial | yes (2) | 21/21 | 4/4 | 436,988/65,715 | $0.092 | 196 | 589 | 11,866 | stages, actions, effects, mechanisms, events, outputs, expressions, functions.collections, views, inputs, population, types, clock, end, policies |
| labor_negotiation | yes (3) | 18/18 | 4/4 | 550,163/56,480 | $0.083 | 160 | 480 | 7,693 | stages, actions, effects, population, outputs, functions.collections, policies |
| sealed_tender | yes (2) | 3/3 | 4/4 | 155,363/21,684 | $0.022 | 393 | 333 | 11,997 | records, mechanisms, stages, actions, market.auction, groups.roles, social.channels, views, functions.collections, functions.random, effects, events |
| sports_league | yes (2) | 12/12 | 4/4 | 293,162/40,187 | $0.041 | 485 | 540 | 11,184 | guide(), functions.random, functions.collections, stages, effects, actions, events, views, outputs, expressions, inputs, population, defs, entities, metrics, end, invariants, brief, policies |
| town_hall | yes (3) | 18/18 | 4/4 | 334,218/38,627 | $0.022 | 506 | 377 | 11,884 | stages, actions, records, outputs, views, mechanisms, expressions, types, effects, decision.ballot, functions.collections, defs, inputs, policies |
| treaty | yes (1) | 9/9 | 4/4 | 231,426/25,796 | $0.021 | 146 | 296 | 15,604 | guide, patterns, mechanisms, functions.random, types, entities, population, inputs, actions, stages, views, events, outputs, end, records, effects, functions.collections, expressions, outputs, policies |
| weekly_inventory | yes (2) | 9/9 | 4/4 | 436,502/34,991 | $0.046 | 302 | 354 | 12,576 | patterns, functions.random, actions, stages, effects, events, outputs, policies, inputs, patterns.draw, world, types, population, views, metrics, guide |
| werewolf | yes (2) | 3/3 | 4/4 | 471,079/40,548 | $0.062 | 290 | 355 | 15,694 | mechanisms, patterns, actions, stages, groups.roles, effects, views, events, functions.collections, functions.random, end, expressions, records, types, population, inputs, blocks, outputs |

**Aggregate:** clean check 12/12 · all runs ok 12/12 · fidelity 50/50 (100%) · fidelity of the contract `fg_env.author` saves 50/50 (100%) · tokens 4,126,615 in / 713,471 out · $0.582

## Check errors authors hit (most frequent first)

- 6× `syntax error: invalid syntax` — e.g. `stages[0].who: syntax error: invalid syntax → expression: $round % 2 == 1 ? $entity(union_leader) : $entity(company_leader)`
- 1× `'…' is not an agent type` — e.g. `brief.roles.house: 'house' is not an agent type → set types.house.agent: true`
- 1× `cannot read '…' of str '…' — in `$lot.high_bidder.id` (smoke run of N round(s), random agents)` — e.g. `events[2].do[1].then[2]: cannot read '.id' of str 'collector_1' — in `$lot.high_bidder.id` (smoke run of 2 round(s), random agents) → fix the rule at this path (found by a smoke run)`
- 1× `must be text, got a list` — e.g. `views.department.sort: must be text, got a list → see guide('views')`
- 1× `template '…': expected a number, got str '…' — in `$round - arrival_round` (smoke run of N round(s), random agents)` — e.g. `views.queue: template '{name}: urgency {urgency}, waiting {$round - arrival_round} h, arrived in hour {arrival_round}': expected a number, got str 'arrival_round' — in `$round - arrival_round` (smoke run of 1 round(s), r`
- 1× `'…' is a built-in entity field` — e.g. `types.exhibit.props.name: 'name' is a built-in entity field → remove name from props; set name on the entity or population entry, outside props`
- 1× `cannot read '…' of str '…' — in `$world.proposal != null and $stage.name == respond` (smoke run of N round(s), policy '…' playing union_negotiator)` — e.g. `policies.union_hard.rules[0]: cannot read '.name' of str 'respond' — in `$world.proposal != null and $stage.name == respond` (smoke run of 1 round(s), policy 'union_hard' playing union_negotiator) → fix the rule at this `
- 1× `cannot read '…' of str '…' — in `$world.proposal != null and $stage.name == respond` (smoke run of N round(s), policy '…' playing union_negotiator, company_negotiator)` — e.g. `policies.company_soft.rules[0]: cannot read '.name' of str 'bargain' — in `$world.proposal != null and $stage.name == respond` (smoke run of 2 round(s), policy 'company_soft' playing union_negotiator, company_negotiator)`
- 1× `filters the choices by private colluder of other firm agents: the tool's list of choices would reveal it to the actor` — e.g. `actions.message.params.to.where: filters the choices by private colluder of other firm agents: the tool's list of choices would reveal it to the actor → filter by what the actor may know (public properties, its own, a re`
- 1× `shows private bid of every firm to each reader` — e.g. `views.all_bids.show: shows private bid of every firm to each reader → add a `where` choosing whose to show (e.g. `$it.id == $actor.id`), or leave the private field out`
- 1× `'…' is not a field name; write an expression as {$...}` — e.g. `views.table.show: '{gf - ga}' is not a field name; write an expression as {$...} → template: {$i + 1}. {name} — {played} played, {won}W {drawn}D {lost}L, {gf} for, {ga} against, goal difference {gf - ga}, {points} points`
- 1× `must be a whole number, got the text "$len($inputs.proposals)"` — e.g. `actions.vote.per_round: must be a whole number, got the text "$len($inputs.proposals)" → write a whole number without quotes; see guide('actions')`
- 1× `syntax error: invalid syntax. Perhaps you forgot a comma?` — e.g. `population[0].props.priority: syntax error: invalid syntax. Perhaps you forgot a comma? → expression: $choice([green space, traffic, books, the town budget])`
- 1× `a poisson draw needs `mean`` — e.g. `patterns.demand: a poisson draw needs `mean` → see guide('patterns') for `draw``
- 1× `Input should be '…', '…', '…' or '…'` — e.g. `patterns.noise.dist: Input should be 'normal', 'uniform', 'lognormal' or 'laplace' → see guide('patterns') for `noise``
- 1× ``key` is not a field of a `counts` pattern` — e.g. `patterns.demand.key: `key` is not a field of a `counts` pattern → did you mean 'keys'? it takes: description, unit, keys, table, column, min, max, record, fit, uncertainty, dist, dispersion, every`
- 1× ``mean` is not a field of a `counts` pattern` — e.g. `patterns.demand.mean: `mean` is not a field of a `counts` pattern → it takes: description, unit, keys, table, column, min, max, record, fit, uncertainty, dist, dispersion, every`
- 1× `'…' is not a field here` — e.g. `metrics.cash.format: 'format' is not a field here → 'format' belongs to another part of the contract; remove it here; see guide('metrics')`
- 1× `$actor.world: owner has no property '…'` — e.g. `views.shelf.show: $actor.world: owner has no property 'world' → properties: none — in `$actor.world.stock``
- 1× ``stage` is not a field of `groups` mode `roles`` — e.g. `mechanisms.roles.stage: `stage` is not a field of `groups` mode `roles` → `groups` mode `roles` takes: who, deck, teams, know, alive, reveal, actions, views`

## Failed fidelity checks

