# Authoring benchmark — after-s1

SDK commit `?` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide tokens | guide parts |
|---|---|---|---|---|---|---|---|---|---|
| beer_game | yes (4) | 3/3 | 5/6 | 477,529/127,473 | $0.067 | 1527 | 468 | 12,596 | guide, expressions, effects, functions.collections, stages, actions, views, events, inputs, outputs, population, brief, policies, functions.random, end, defs, types, functions, metrics |
| treaty | yes (2) | 12/12 | 4/4 | 389,036/63,854 | $0.045 | 333 | 426 | 14,781 | patterns, functions.random, types, stages, views, records, effects, brief, actions, functions.collections, inputs, events, population, end, outputs, expressions, policies, metrics, invariants, entities |
| classroom | yes (2) | 12/12 | 4/4 | 215,391/19,669 | $0.016 | 177 | 261 | 9,788 | actions, stages, effects, expressions, functions.collections, functions.random, views, events, outputs, views, policies |
| ed_triage | yes (1) | 6/6 | 4/4 | 522,333/108,914 | $0.063 | 1593 | 380 | 14,025 | patterns, functions.random, effects, stages, actions, views, outputs, functions.collections, events, inputs, patterns.noise, patterns.counts, inspect, guide, overview, population, clock, policies, expressions |

**Aggregate:** clean check 4/4 · all runs ok 4/4 · fidelity 17/18 (94%) · tokens 1,604,289 in / 319,910 out · $0.190

## Check errors authors hit (most frequent first)

- 12× `$actor is not available here` — e.g. `events[1].do[0]: $actor is not available here → available: $arm, $clock, $i, $inputs, $it, $metrics, $pattern, $pending, $physics, $round, $series, $stage, $world — in `$actor.incoming``
- 4× `'…' is not a declared type` — e.g. `brief.roles.retailer: 'retailer' is not a declared type → types: company`
- 2× `'…' is not a field here` — e.g. `types.company.props.upstream.of: 'of' is not a field here → 'of' belongs to another part of the contract; remove it here; see guide('types')`
- 2× `unknown type '…'` — e.g. `types.company.props.upstream.type: unknown type 'entity'`
- 2× `not valid JSON` — e.g. `(contract): not valid JSON`
- 2× `invariant `$sum(company, $it.stock + $it.incoming + $it.production) + $sum($world.customer_demand) == $inputs.start_stock * N + $inputs.start_pipeline * N + $sum($world.factory_orders)` no longer holds after events[N].do (Beer is conserved: everything shipped or produced is in stock, in transit or delivered to customers.) (smoke run of N round(s), random agents)` — e.g. `invariants[2]: invariant `$sum(company, $it.stock + $it.incoming + $it.production) + $sum($world.customer_demand) == $inputs.start_stock * 4 + $inputs.start_pipeline * 5 + $sum($world.factory_orders)` no longer holds aft`
- 2× `must be an object, got the text "text"` — e.g. `inputs.nations.fields.id: must be an object, got the text "text" → write an object {...} with the fields guide('inputs') lists`
- 1× `unknown format '…' (use one of: money, pct, pctN, int, upper, lower, title, yesno, list, N, N, N, N, N)` — e.g. `views.position.show: unknown format 'capitalize' (use one of: money, pct, pct1, int, upper, lower, title, yesno, list, 0, 1, 2, 3, 4) → template: It is week {$round} of {$inputs.weeks}. {$actor.role|capitalize}: stock {s`
- 1× `$series.customer_demand: no such metric` — e.g. `outputs.bullwhip_ratio: $series.customer_demand: no such metric`
- 1× `$series.factory_orders: no such metric` — e.g. `outputs.bullwhip_ratio: $series.factory_orders: no such metric`
- 1× `invariant `$len($world.customer_demand) <= $inputs.weeks and $len($world.factory_orders) <= $inputs.weeks` no longer holds after events[N].do (The game lasts the stated number of weeks.) (smoke run of N round(s), random agents)` — e.g. `invariants[2]: invariant `$len($world.customer_demand) <= $inputs.weeks and $len($world.factory_orders) <= $inputs.weeks` no longer holds after events[0].do (The game lasts the stated number of weeks.) (smoke run of 11 r`
- 1× `must be a whole number, got the text "$inputs.message_max_len"` — e.g. `actions.message.params.text.max_len: must be a whole number, got the text "$inputs.message_max_len" → write a whole number without quotes; see guide('actions')`
- 1× `'…' needs a subject; write {$actor.effort} or similar` — e.g. `actions.study.outcome: '{effort}' needs a subject; write {$actor.effort} or similar → template: You decide to {effort} this week.`
- 1× `Input should be '…' or '…'` — e.g. `patterns.arrival_process.dist: Input should be 'poisson' or 'negative_binomial' → see guide('patterns') for `counts``

## Failed fidelity checks

- **beer_game** `beer_costs_add_up`: roles: ['filler', 'retailer', 'wholesaler', 'distributor', 'factory']
