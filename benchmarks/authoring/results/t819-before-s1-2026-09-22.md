# Authoring benchmark — before-s1

SDK commit `?` · model `deepseek/deepseek-v4.1-flash`

| brief | clean check (writes) | runs ok | fidelity | tokens in/out | cost | wall s | lines | guide tokens | guide parts |
|---|---|---|---|---|---|---|---|---|---|
| beer_game | yes (1) | 3/3 | 6/6 | 535,114/71,593 | $0.051 | 713 | 392 | 25,428 | all, effects, events, functions, patterns, stages, metrics, actions, population, inputs, views, mechanisms, functions.collections, types, world, outputs, invariants, brief, clock, functions.stats, expressions, entities, model, templates, authoring, end, records |
| treaty | yes (2) | 9/9 | 3/4 | 582,120/57,924 | $0.047 | 578 | 330 | 22,496 | all, patterns, types, functions, stages, functions.collections, records, end, outputs, events, actions, population, inputs, world, views, expressions, effects, functions.world, metrics, brief, triggers, mechanisms, authoring, policies |
| classroom | yes (1) | 9/9 | 4/4 | 552,718/81,941 | $0.043 | 1381 | 435 | 19,903 | all, expressions, effects, stages, events, functions.collections, views, outputs, metrics, types, inputs, population, brief, records, actions, functions, model, templates, authoring, policies |
| ed_triage | yes (2) | 0/3 | 0/4 | 573,437/62,485 | $0.035 | 1060 | 568 | 17,242 | all, patterns, functions, functions.collections, effects, population, inputs, events, actions, stages, outputs, metrics, types, views, patterns.random, patterns.observation, functions.random, functions.world |

**Aggregate:** clean check 4/4 · all runs ok 3/4 · fidelity 13/18 (72%) · tokens 2,243,389 in / 273,943 out · $0.175

## Check errors authors hit (most frequent first)

- 2× `'…' is not a field here` — e.g. `policies.moderate.description: 'description' is not a field here → 'description' belongs to another part of the contract; remove it here`
- 2× `wrong number of arguments: $lookup(table, field, key)` — e.g. `outputs.lookup: wrong number of arguments: $lookup(table, field, key) → in `$lookup($dict($inputs.treatment, $it.urgency, $it.hours), 3)``
- 2× `Input should be a valid list` — e.g. `patterns.urgency.values: Input should be a valid list → see guide('patterns') for `draw``
- 1× `syntax error: invalid syntax` — e.g. `outputs.bullwhip_ratio: syntax error: invalid syntax → expression: $len($entity('factory').order_hist) > 1 and $len($world.demand_hist) > 1 ? (($sum($map($entity('factory').order_hist, $it * $it)) / $len($entity('factory`
- 1× `must be text, got an object` — e.g. `records.messages.fields.text: must be text, got an object`
- 1× `invariants[N]: invariant `$count($unique($map(nation, $it.red_line))) == $count(nation)` no longer holds after build (Every nation has a different red line.)` — e.g. `invariants[1]: invariants[1]: invariant `$count($unique($map(nation, $it.red_line))) == $count(nation)` no longer holds after build (Every nation has a different red line.) → fix the rule at this path (found by a smoke r`
- 1× `'…' needs a subject; write {$actor.topic} or similar` — e.g. `actions.teach.outcome: '{topic}' needs a subject; write {$actor.topic} or similar → template: Teaching {topic}.`
- 1× `'…' needs a subject; write {$actor.effort} or similar` — e.g. `actions.study.outcome: '{effort}' needs a subject; write {$actor.effort} or similar → template: effort {effort} → {$actor.s3}`
- 1× `must be a number, got the text "$inputs.max_score"` — e.g. `types.student.props.last_score.max: must be a number, got the text "$inputs.max_score" → write a number without quotes`
- 1× `Input should be '…', '…', '…', '…', '…', '…', '…', '…' or '…'` — e.g. `patterns.urg.dist: Input should be 'normal', 'lognormal', 'uniform', 'beta', 'gamma', 'triangular', 'choice', 'mvnormal' or 'poisson' → see guide('patterns') for `draw``
- 1× ``n` is not a field of a `draw` pattern` — e.g. `patterns.urg.n: `n` is not a field of a `draw` pattern → it takes: description, unit, keys, table, column, min, max, record, fit, uncertainty, dist, mean, sd, mu, sigma, low, high, mode, a, b, shape, scale, values, weigh`
- 1× ``s` is not a field of a `draw` pattern` — e.g. `patterns.urg.s: `s` is not a field of a `draw` pattern → did you mean 'sd'? it takes: description, unit, keys, table, column, min, max, record, fit, uncertainty, dist, mean, sd, mu, sigma, low, high, mode, a, b, shape, s`
- 1× `not valid JSON` — e.g. `(contract): not valid JSON`

## Failed fidelity checks

- **treaty** `treaty_red_lines_stay_secret`: suria was shown estavia's red line 28: …e them.

Since your last turn:
- Westmark: «zqwestmark3text»
- Estavia ratifies the 28% cut.

Proposal: On the table: a 28% cut proposed by suria. Rati…
- **ed_triage** `outputs_present`: random runs fail
- **ed_triage** `ed_patients_are_conserved`: random runs fail
- **ed_triage** `ed_arrivals_are_exogenous`: random runs fail
- **ed_triage** `ed_idle_staff_treat_nobody`: random runs fail
- **ed_triage** run random seed 1 failed: ContractError: contract is invalid (2 error(s)):
  - patterns.urgency.values: Input should be a valid list → see guide('patterns') for `draw`
  - patterns.urgency.weights: Input should be a valid list → see guide('patterns') for `draw`
- **ed_triage** run random seed 2 failed: ContractError: contract is invalid (2 error(s)):
  - patterns.urgency.values: Input should be a valid list → see guide('patterns') for `draw`
  - patterns.urgency.weights: Input should be a valid list → see guide('patterns') for `draw`
- **ed_triage** run random seed 3 failed: ContractError: contract is invalid (2 error(s)):
  - patterns.urgency.values: Input should be a valid list → see guide('patterns') for `draw`
  - patterns.urgency.weights: Input should be a valid list → see guide('patterns') for `draw`
