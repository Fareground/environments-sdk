# Environments SDK documentation

Start with the [documentation home](sdk/index.md), [business quickstart](sdk/getting-started.md), or [generated reference](sdk/reference.md).

The `sdk/` directory documents the current JSON contract API. Older template-engine documents in this directory are retained for `fg_env.legacy` users.
