"""Copies of a run: branches to look ahead on (:mod:`.branch`), forks under changes (:mod:`.forks`), replays
(:mod:`.replay`), snapshots (:mod:`.snapshot`), previews (:mod:`.previews`), and copies stepped or piloted by a caller
(:mod:`.stepping`, :mod:`.pilot`, copied directly by :mod:`.direct`)."""
