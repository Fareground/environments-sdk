"""Authoring: :func:`fg_env.author` (a model writes a contract from a brief, :mod:`.author`, checked in a
:mod:`.sandbox`) and :func:`fg_env.new` (ready-to-run starting contracts, :mod:`.scaffold`)."""
