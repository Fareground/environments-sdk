"""Authoring: :func:`fg_env.author` (a model writes a contract from a brief, :mod:`.author`, with the tools of a
:mod:`.workbench`; every revision is tested by :mod:`.testing` in a :mod:`.sandbox`) and :func:`fg_env.new`
(ready-to-run starting contracts, :mod:`.scaffold`)."""
