"""World patterns: seasons, trends, responses, random processes, draws, observation and memory, declared under
``patterns`` and read as ``$pattern.<name>``. See :mod:`.base` for the shapes and :mod:`.runtime` for how they run."""
from __future__ import annotations

from .base import GROUPS, KINDS, PatternConfig

__all__ = ["GROUPS", "KINDS", "PatternConfig"]
