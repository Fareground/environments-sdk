"""The live world of one run (:mod:`.live`) and its parts: entities, properties, links, records, the event log and
the space (geometry, positions, layers); :mod:`.build` builds the starting world from a contract."""
