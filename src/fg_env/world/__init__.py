"""The world of one run (:mod:`.store`), how expressions read it (:mod:`.evaluation`) and its parts: entities,
properties, links, records, the event log and the space (geometry, positions, layers); :mod:`.build` builds the starting
world from a contract."""
