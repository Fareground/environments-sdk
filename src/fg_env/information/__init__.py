"""What an agent is shown or offered, and what spectators see: one component, :class:`~.core.Information`.

Its brief, update and news, and the views it looks at (:mod:`.perception`, with the news index in :mod:`.news_index`);
what inspecting an entity shows it and its [id] handles (:mod:`.reads`); the tools it is offered — their JSON Schemas
(:mod:`.schemas`) and descriptions (:mod:`.tool_text`); what an action's announcement may repeat of its arguments
(:mod:`.announce`); spectator views and frames; and the record of what each agent was shown (:mod:`.exposure`).
"""
