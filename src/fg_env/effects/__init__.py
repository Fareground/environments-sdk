"""Effects: how actions, events and stages change the world. :mod:`.runner` applies effect lists; :mod:`.chance`
and :mod:`.layer` hold the `chance` and `layer` effects; :mod:`.captures` keeps what deferred rules read."""
