"""Running a loaded environment: the engine (:mod:`.env`), its round loop (:mod:`.rounds`), stages (:mod:`.stages`) and
turns (:mod:`.turn`, :mod:`.session`, :mod:`.driving`); what the world does on its own (:mod:`.happenings`); what agents
perceive (:mod:`.perception`); the checks, diagnosis and measurements a run makes as it plays; budgets and results."""
