"""Running a loaded environment: the run's facade (:mod:`.env`) and its state (:mod:`.state`); the rules that evaluate
and commit world logic (:mod:`.rules`, with events in :mod:`.events`); the schedule that says when everything happens
and who acts (:mod:`.schedule`); turns (:mod:`.turn`, :mod:`.ledger`, :mod:`.sealed`, :mod:`.session`,
:mod:`.driving`); what agents perceive (:mod:`.perception`); the diagnosis and measurements a run makes as it plays;
budgets and results."""
